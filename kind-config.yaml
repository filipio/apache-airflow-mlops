kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
  - role: worker # commented 2 workers are too much for my PC when running two clusters at once ~Wojtek
  - role: worker # commented, 3 workers can be too much for a PC
  - role: worker
